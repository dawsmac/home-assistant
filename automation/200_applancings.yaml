- id: '200'
  alias: Alert Washing Machine Done
  description: ''
  trigger:
    - platform: numeric_state
      entity_id: sensor.washing_machine_current
      above: '.75'
  condition: []
  action:
    - wait_for_trigger:
        - platform: numeric_state
          entity_id: sensor.washing_machine_current
          below: '.3'
          for: '00:10'
      continue_on_timeout: false
    - service: notify.notify
      data:
        title: Washing machine
        message: 'Washing machine done, ready for the dryer'
    - service: tts.google_translate_say
      data:
        entity_id: all
        message: 'Washing machine done, ready for the dryer'
  mode: restart

- id: '201'
  alias: Alert Tumble Dryer Done
  trigger:
    - platform: numeric_state
      entity_id: sensor.tumble_dryer_current
      above: '.75'
  condition: []
  action:
    - wait_for_trigger:
        - platform: numeric_state
          entity_id: sensor.tumble_dryer_current
          below: '.3'
          for: '00:10'
      continue_on_timeout: false
    - service: notify.notify
      data:
        title: Tumble dryer machine
        message: 'Tumble dryer is done, ready to be sorted'
    - service: tts.google_translate_say
      data:
        entity_id: all
        message: 'Tumble dryer is done, ready to be sorted'
  mode: restart


- id: '202'
  alias: Alert Dishwasher Done
  trigger:
    - platform: numeric_state
      entity_id:   sensor.dishwasher_current
      above: '.75'
  condition: []
  action:
    - wait_for_trigger:
        - platform: numeric_state
          entity_id: sensor.dishwasher_current
          below: '.3'
          for: '00:10'
      continue_on_timeout: false
    - service: notify.notify
      data:
        title: Dishwasher alert!
        message: 'Dishwasher is done, ready to be emptied'
    - service: tts.google_translate_say
      data:
        entity_id: all
        message: 'Dishwasher is done, ready to be emptied'
  mode: restart

- id: '203'
  alias: Morning Turn on applances
  trigger:
    - platform: state
      entity_id: binary_sensor.morning_alarm
      to: 'on'
  condition:
    - condition: time
      after: '05:00'
      before: '09:00'
    - condition: state
      entity_id:
        - input_select.house_mode
      state: 'Night'
    - condition: state
      entity_id: group.persons
      state: 'home'
  action:
    - service: scene.turn_on
      data: {}
      target:
        entity_id: scene.appliances